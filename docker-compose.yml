services:
  workspace:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    working_dir: /workspaces/valorem
    volumes:
      - .:/workspaces/valorem
    tty: true
    stdin_open: true
    ports:
      - "3000:3000"
      - "8899:8899"
      - "8900:8900"
